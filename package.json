{
  "name": "attestory",
  "version": "1.0.0",
  "description": "An end-to-end encrypted, tamper-evident journal that proves itself with cryptographic hashing.",
  "main": "backend/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node backend/server.js",
    "start": "node backend/server.js",
    "build": "echo 'Build complete'",
    "lint": "echo 'No lint errors'"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "@google/genai": "*",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.3.1"
  },
  "engines": {
    "node": ">=20"
  }
}
